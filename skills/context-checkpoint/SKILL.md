---
name: context-checkpoint
description: Compatibility alias for the current repo checkpoint command
---

# context-checkpoint

Alias for `ck`.

Use the same canonical command:

```powershell
cont
```

For a one-shot save+resume flow, use `cont`.
